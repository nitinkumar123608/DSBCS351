#include <stdio.h>

int fib(int n) {
    if (n <= 1) return n; // Base cases: fib(0) = 0, fib(1) = 1
    return fib(n - 1) + fib(n - 2); 
}

int main() {
    int n = 6;
    printf("%dth Fibonacci number is %d\n", n, fib(n)); 
    return 0;
}
